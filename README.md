# start

```
$ git clone https://github.com/masum/pollingGitlab_postMattermost.git
$ npm install
$ export GITLAB_ID={id}
$ export GITLAB_PW={password}
$ export GITLAB_HOST={host}
$ export GITLAB_PROJECT={project}
$ export GITLAB_GROUP={group}
$ export GITLAB_REPO={repository}
$ export GITLAB_ICON={Redmine Icon URL}
$ export MATTERMOST_HOST={Mattermost host}
$ export MATTERMOST_PORT={Mattermost port}
$ export MATTERMOST_PATH={Matermost path}

$ npm start
```

# last post
saved ```./lasttime.txt```


